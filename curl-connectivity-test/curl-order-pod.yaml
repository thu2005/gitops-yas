apiVersion: v1
kind: Pod
metadata:
  name: curl-order
  namespace: yas-dev
spec:
  serviceAccountName: order
  restartPolicy: Never
  containers:
    - name: curl
      image: curlimages/curl:latest
      command: ["sh", "-c", "sleep 3600"]
