{{- if .Values.istio.enabled }}
---
apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: yas-gateway
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*"
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: yas-web
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - storefront.yas.local
    - backoffice.yas.local
    - swagger.yas.local
  gateways:
    - yas-gateway
  http:
    - match:
        - authority:
            exact: storefront.yas.local
          uri:
            prefix: /api
        - authority:
            exact: storefront.yas.local
          uri:
            prefix: /oauth2
        - authority:
            exact: storefront.yas.local
          uri:
            prefix: /login
        - authority:
            exact: storefront.yas.local
          uri:
            prefix: /logout
      route:
        - destination:
            host: storefront-bff.{{ .Release.Namespace }}.svc.cluster.local
            port:
              number: 80
    - match:
        - authority:
            exact: storefront.yas.local
      route:
        - destination:
            host: storefront-ui.{{ .Release.Namespace }}.svc.cluster.local
            port:
              number: 3000
    - match:
        - authority:
            exact: backoffice.yas.local
          uri:
            prefix: /api
        - authority:
            exact: backoffice.yas.local
          uri:
            prefix: /oauth2
        - authority:
            exact: backoffice.yas.local
          uri:
            prefix: /login
        - authority:
            exact: backoffice.yas.local
          uri:
            prefix: /logout
      route:
        - destination:
            host: backoffice-bff.{{ .Release.Namespace }}.svc.cluster.local
            port:
              number: 80
    - match:
        - authority:
            exact: backoffice.yas.local
      route:
        - destination:
            host: backoffice-ui.{{ .Release.Namespace }}.svc.cluster.local
            port:
              number: 3000
    - match:
        - authority:
            exact: swagger.yas.local
      route:
        - destination:
            host: swagger-ui.{{ .Release.Namespace }}.svc.cluster.local
            port:
              number: 8080
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: identity-web
  namespace: {{ .Release.Namespace }}
spec:
  gateways:
    - yas-gateway
  hosts:
    - identity
    - identity.yas.local.com
    - "*"
  http:
    - match:
        - authority:
            exact: identity
        - authority:
            exact: identity.yas.local.com
        - uri:
            prefix: /
      route:
        - destination:
            host: keycloak-service.keycloak.svc.cluster.local
            port:
              number: 80
---
# Retry policy for tax service.
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: tax-retry
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - tax
  http:
    - retries:
        attempts: 3
        perTryTimeout: 5s
        retryOn: "5xx,retriable-4xx,connect-failure,reset"
      timeout: 30s
      route:
        - destination:
            host: tax
            port:
              number: 80
---
# Retry policy for product service.
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: product-retry
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - product
  http:
    - retries:
        attempts: 3
        perTryTimeout: 5s
        retryOn: "5xx,connect-failure,reset"
      timeout: 30s
      route:
        - destination:
            host: product
            port:
              number: 80
---
# Mock HEAD request to Elasticsearch to fix Spring Data ES client 9.x compatibility issue with ES 8.x
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: elasticsearch-mock
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - "elasticsearch-es-http.elasticsearch.svc.cluster.local"
    - "elasticsearch-es-http.elasticsearch"
    - "elasticsearch"
  http:
    - match:
        - method:
            regex: "^(HEAD|GET)$"
      route:
        - destination:
            host: elasticsearch-es-http.elasticsearch.svc.cluster.local
            port:
              number: 9200
      headers:
        request:
          set:
            Accept: "application/json"
          remove:
            - Content-Type
    - route:
        - destination:
            host: elasticsearch-es-http.elasticsearch.svc.cluster.local
            port:
              number: 9200
      headers:
        request:
          set:
            Accept: "application/json"
            Content-Type: "application/json"
{{- end }}
